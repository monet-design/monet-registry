아래 이미지 url들에 대해서 다음 작업 수행하라.
진행상황은 @footer-checklist.yaml로 관리한다.

1. 체크리스트로부터 아직 구현되지 않은 이미지들을 파악한다.
2. img-to-component agent를 병렬로 호출해 1번 단계의 이미지들만을 대상으로 컴포넌트로 구현한다. 한번에 최대 20개씩만 병렬로 호출한다. 구현 완료되면 완료 체크한 뒤 다음 batch를 반복한다.
